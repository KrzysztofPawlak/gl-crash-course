<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:bind="http://schemas.android.com/tools">
    <data>
        <variable
                name="name"
                type="String"/>

        <variable
                name="position"
                type="String"/>

        <variable
                name="avatar"
                type="String"/>

        <variable name="id" type="Integer"/>

        <variable
                name="listener"
                type="com.example.gl_crash_course.memberslist.MemberAdapter.OnMemberClickListener"/>
    </data>
    <com.google.android.material.card.MaterialCardView
            android:id="@+id/list_item"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="@{() -> listener.onMemberClick(id)}">>
        <RelativeLayout android:layout_width="match_parent" android:layout_height="wrap_content">

            <ImageView
                    android:id="@+id/iv"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_alignParentTop="true"
                    android:src="@mipmap/ic_launcher" android:contentDescription="TODO"
                    android:imageUrl="@{avatar}"/>
            <TextView
                    android:id="@+id/tvName"
                    android:text="@{name}"
                    android:layout_width="match_parent"
                    android:layout_toRightOf="@+id/iv"
                    android:layout_height="wrap_content"/>
            <TextView
                    android:id="@+id/tvPosition"
                    android:text="@{position}"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_toRightOf="@+id/iv"
                    android:layout_below="@+id/tvName"/>
        </RelativeLayout>
    </com.google.android.material.card.MaterialCardView>
</layout>